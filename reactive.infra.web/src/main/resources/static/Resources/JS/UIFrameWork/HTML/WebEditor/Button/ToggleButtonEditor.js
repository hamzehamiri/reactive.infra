import {WebEditor} from "../Common/WebEditor.js";

export default class ToggleButtonEditor extends WebEditor {

    static registerKey() {
        return "ToggleButtonEditor";
    };

    constructor() {
        super();
    }
}