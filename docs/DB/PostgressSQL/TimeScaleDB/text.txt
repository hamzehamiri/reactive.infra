https://docs.timescale.com/self-hosted/latest/install/installation-windows/#supported-platforms
https://docs.timescale.com/self-hosted/latest/install/installation-windows/#add-the-timescaledb-extension-to-your-database

CREATE TABLE conditions (
                            time        TIMESTAMPTZ       NOT NULL,
                            location    TEXT              NOT NULL,
                            device      TEXT              NOT NULL,
                            temperature DOUBLE PRECISION  NULL,
                            humidity    DOUBLE PRECISION  NULL
);

CREATE EXTENSION IF NOT EXISTS timescaledb;

SELECT create_hypertable('conditions', by_range('time'));